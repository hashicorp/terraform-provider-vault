version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:2022.03
    steps:
      - run: sudo apt-get update && sudo apt-get install -y figlet
      - run: figlet 'Moved to GitHub Actions'
      - run: echo https://github.com/hashicorp/terraform-provider-vault/actions
